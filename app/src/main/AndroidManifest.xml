<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.belief">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".MvpApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.sport.ShowClassActivity"></activity>
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.comm.CommDetailActivity"></activity>
        <activity android:name=".ui.user.LoginActivity"></activity>
        <activity android:name=".ui.sport.ClassDetailActivity"></activity>
        <activity android:name=".ui.sport.StartSportActivity"></activity>
        <activity android:name=".ui.user.PersonalinfoActivity"></activity>
        <activity android:name=".ui.user.UpdateInfoActivity"></activity>
        <activity android:name=".ui.sport.AddShareActivity"></activity>
    </application>

</manifest>